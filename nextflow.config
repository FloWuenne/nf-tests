wave.enabled = true
fusion.enabled = true
process.scratch = false
docker {
  enabled = true
  runOptions = '-v $HOME/.aws/credentials:/credentials -e AWS_SHARED_CREDENTIALS_FILE=/credentials -e WAVE_ENTRY_CHAIN=unshare'
}
